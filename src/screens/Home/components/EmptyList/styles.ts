import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderTopColor: '#808080',
    borderTopWidth: 0.5,
    alignItems: 'center',
  },
  imgClip: {
    width: 56,
    height: 56,
    marginTop: 48,
    marginBottom: 16,
  },
  emptyText: {
    fontSize: 14,
    color: '#808080',
  },
  emptyTitle: {
    fontWeight: 'bold',
  },
});
